① 预测结果写 PG 的时机和主体（最关键）
A：icewash 在 pipeline 跑完、xlsx 落盘的同时，顺手把结果也 INSERT 进 PG forecast 表（icewash 主动写，backend 只读）

② 网络可达性 — PG 在 127.0.0.1，icewash 在哪跑？icewash 和 backend/PG 是同一台机不同 docker 容器运行，内网分离，整个项目只有一个PG容器，后端容器和icswash容器，他们都是容器间通信，未来dockercompose的时候再组到一张network下面

③ get_forecast_result 的读取时序
要做"completed 后数据必然已落库"的保证（写库在标记 completed 之前完成

④ 历史 CSV 谁灌、何时灌
一次性灌入不动

⑤ agent 异步轮询会不会超时
rounds 要为"轮询等待"开绿灯


    