# T03 · 能力域+聊天域数据模型并入 PG（domain-pg-models）

- 任务 ID：T03
- **标题与目标**：把 frontend-ref/backend-ref 工作台表、icewash `pg_sync.py` 写入的 `fcst_*` 中转表、forecast/attribution/what-if 语义表，以及聊天域和用户扩展字段一次性落到 backup 壳的 PG；产出 Alembic 0003 迁移，确保所有后续计划使用同一 PG 数据源。
- **关联文档章节**：feat-icewash.md §11.2（存储并入 PG，聊天域不新表）；backend-backup models/*、backend-ref models.py、services/icewash-model/cbg_fcst_month/pg_sync.py
- 前置依赖 blockedBy：T01

## 问题
- 任务 T03 当前需要完成本计划标题对应的交付；旧版计划结构无法被 plan-generator v1.2.0 的结构化校验直接读取。

## 决策
- 保留本计划既有技术边界、参数、实施内容和验收命令；仅补齐 v1.2.0 要求的元数据、章节与结构化执行入口。

## 范围
- 包含：本计划既有实施要点、涉及路径、接口、数据约束和验收项。
- 不包含：改变任务目标、blockedBy 依赖、代码实现或项目业务口径。

## 风险与回滚
- 风险：旧计划正文中的命令或外部服务依赖可能在执行环境中不可用，导致该任务验收失败。
- 回滚：执行前保存本任务涉及文件的补丁；失败时使用 `git apply -R /tmp/T03-plan.patch` 反向应用补丁并恢复前一阶段。

## 实施步骤

### 步骤 1：执行原计划实施内容
- 对象：T03 对应的目标模块、接口和验收对象。
- 动作：按下方保留的原实施内容执行，并使用其中的命令完成验证。
- 参数：严格使用正文中给出的路径、端口、字段、超时、权限和命令参数。
- 文件：docs/plans/T03-domain-pg-models-2026-09-01.md
- 命令：
  ```bash
  python /c/Users/jie32.guo/.codex/skills/plan-generator/scripts/plan_state.py lint-plan --plan docs/plans/T03-domain-pg-models-2026-09-01.md
  ```
#### 1. 新建工作台 ORM 模型 `backend/src/app/models/relay.py`
- 新增三张 frontend-ref 工作台实际查询表，字段与 backend-ref `models.py` 对齐，`id` 使用壳的 `IdMixin`（`String(36)` UUID）：
  - `WorkbenchDatasetRow`（`workbench_dataset_rows`）：`dataset:String(32,index)`、`period/category/channel_l3/sku/version/series:String`（可空、索引）、`payload:JSONB`。建立 `ix_wb_dataset_category_period`、`ix_wb_dataset_channel_sku`、`ix_wb_dataset_category_version`。
  - `AttributionAnalysisRow`（`attribution_analysis_rows`）：`version/category/period/horizon/sku/status/series/channel_l1/channel_l3/attr_type:String`、`y_pred/qty_lag1/impact:Float`、`payload:JSONB`。建立 `ix_attr_version_category`、`ix_attr_sku_period`、`ix_attr_channels`。
  - `ForecastHistoryRow`（`forecast_history_rows`）：`version/category/period/sku/status/series/channel_l1/channel_l3:String`、`retail_qty/retail_amt:Float`、`payload:JSONB`。建立 `ix_fhist_version_category`、`ix_fhist_sku_period`、`ix_fhist_channels`。
- `payload` 统一使用壳 `models/types.py` 的 PG `JSONB` 类型。

#### 2. 新建 icewash 中转 ORM 模型 `backend/src/app/models/fcst_relay.py`
- 建立 3 张与 `services/icewash-model/cbg_fcst_month/pg_sync.py` 列名完全一致的表，`id` 使用 `BIGINT GENERATED BY DEFAULT AS IDENTITY`，使 pandas `to_sql` 不提供 `id` 时仍可插入：
  - `FcstForecastResult`（`fcst_forecast_result`）：`system_forecast_number:String(128)`、`horizon:String(16)`、`forecast_month:String(32)`、`category:String(64)`、`series:String(64)`、`status:String(64)`、`channel_l3:String(128)`、`sku:String(128)`、`final_value:Float`。
  - `FcstAttribution`（`fcst_attribution`）：`system_forecast_number:String(128)`、`horizon:String(16)`、`forecast_month:String(32)`、`category:String(64)`、`series:String(64)`、`status:String(64)`、`channel_l3:String(128)`、`sku:String(128)`、`baseline_model:String(64)`、`y_pred/delta_y/value_T/value_T_1/shap_value/type_impact/contribution_pct/shap_base:Float`、`factor_layer/factor_name/factor_type:String(128)`、`shap_base_month:String(32)`。
  - `FcstHistory`（`fcst_history`）：`period:String(32)`、`category:String(64)`、`channel_l3:String(128)`、`sku:String(128)`、`qty:Float`。
- 所有业务列允许为空；建立 `fcst_forecast_result(system_forecast_number, forecast_month, category)`、`fcst_attribution(system_forecast_number, forecast_month, category, sku)`、`fcst_history(category, sku, channel_l3, period)` 查询索引。

#### 3. 新建 backend-ref 语义 ORM 模型 `backend/src/app/models/forecast_domain.py`
- 新增方案 §11.2 要求的 4 张语义表，字段与 backend-ref `models.py` 对齐：
  - `ForecastRun`（`forecast_runs`）：`id:String(36)`、`product_id:String(36) nullable`、`model_version:String(64)`、`horizon:Integer`、`params:JSONB nullable`、`created_at:DateTime`。
  - `ForecastPoint`（`forecast_points`）：`id:String(36)`、`run_id:String(36,index)`、`period:String(16)`、`qty_p50/qty_p10/qty_p90:Float`。
  - `AttributionResult`（`attribution_results`）：`id:String(36)`、`product_id:String(36) nullable`、`forecast_run_id:String(36) nullable`、`period:String(16) nullable`、`factors:JSONB`、`created_at:DateTime`。
  - `WhatIfScenario`（`whatif_scenarios`）：`id:String(36)`、`product_id:String(36) nullable`、`assumptions/baseline/scenario:JSONB`、`delta_pct:Float`、`summary:Text`、`created_at:DateTime`。
- 本任务不创建 backend-ref 的 `products`/`sales_history` 表，也不添加外键；`product_id`、`run_id`、`forecast_run_id` 保留为业务关联字段，避免引入方案未要求的产品主数据表。

#### 4. 聊天域加列（不新表）
- `models/message.py` 的 `Message` 增加 `result_envelope:JSONB nullable`，助手消息写入 `AgentResultEnvelope.model_dump()`。
- `models/conversation.py` 的 `Conversation` 增加 `agent_conversation_id:String(128) nullable index`、`memory_summary:Text nullable`、`memory_slots:JSONB nullable`、`summary_upto_id:String(36) nullable`。
- `models/user.py` 的 `User` 增加 `oa:String(128) nullable unique index`。

#### 5. `models/__init__.py` 汇聚
- 导出 `WorkbenchDatasetRow`、`AttributionAnalysisRow`、`ForecastHistoryRow`、`FcstForecastResult`、`FcstAttribution`、`FcstHistory`、`ForecastRun`、`ForecastPoint`、`AttributionResult`、`WhatIfScenario`。

#### 6. Alembic 0003 迁移 `backend/alembic/versions/0003_relay_and_chat_extension.py`
- 设置 `revision="0003"`、`down_revision="0002_conversation_pin"`。
- `upgrade()` 创建步骤 1~3 的 10 张表及全部索引，并为 `messages` 增加 `result_envelope`、为 `conversations` 增加 4 列、为 `users` 增加 `oa` 唯一索引。
- `downgrade()` 按逆序删除 10 张表、索引和扩展列，回滚目标为 `0002_conversation_pin`。

#### 7. 对齐 icewash 写入方式
- 修改 `services/icewash-model/cbg_fcst_month/pg_sync.py` 的 `sync_history_csv`：在事务内执行 `DELETE FROM fcst_history`，再以 `if_exists="append"` 写入，保留 Alembic 创建的 identity 列和索引；预测、归因写入继续使用 `append`。
- 用 `BACKEND_PG_URL=postgresql+psycopg2://app:app@127.0.0.1:5432/agent_platform` 验证 3 个 DataFrame 写入时不提供 `id` 也能成功。

#### 8. 剔除 backend-ref SQLite 痕迹
- backend-ref 的 `app/db.py`（aiosqlite/PRAGMA）不复制进 backend；后续能力域服务统一使用壳 `app/database.py` 的 `get_session`。
- `grep -rn "aiosqlite\|PRAGMA\|json_each" backend/src` 零命中；PG JSON 查询由后续服务计划使用 `jsonb_each`。

## 完成标准
- 验收命令：
  ```bash
  python /c/Users/jie32.guo/.codex/skills/plan-generator/scripts/plan_state.py lint-plan --plan docs/plans/T03-domain-pg-models-2026-09-01.md
  ```
- 通过条件：命令退出码为 0，且本节保留的原验收项全部满足。
- [ ] `cd backend && uv run alembic upgrade head` 后，`psql agent_platform -c "\dt"` 含 `workbench_dataset_rows`、`attribution_analysis_rows`、`forecast_history_rows`、`fcst_forecast_result`、`fcst_attribution`、`fcst_history`、`forecast_runs`、`forecast_points`、`attribution_results`、`whatif_scenarios`。
- [ ] `psql agent_platform -c "\d messages"` 含 `result_envelope`（jsonb）；`\d conversations` 含 `agent_conversation_id/memory_summary/memory_slots/summary_upto_id`；`\d users` 含 `oa`。
- [ ] `uv run alembic downgrade -1` 再 `upgrade head` 可逆无报错。
- [ ] `uv run pytest tests/test_models.py` 通过，并且 `python -c "from app.models import WorkbenchDatasetRow, FcstForecastResult, ForecastRun"` 成功。
- [ ] 在 `services/icewash-model/cbg_fcst_month` 执行 `BACKEND_PG_URL=postgresql+psycopg2://app:app@127.0.0.1:5432/agent_platform python -c "import pg_sync; print('pg_sync imports ok')"` 成功，且 `fcst_*` 表可接受不含 `id` 的写入。
- [ ] `grep -rn "aiosqlite\|PRAGMA\|json_each" backend/src` 零输出。
